services:
  ai-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${AI_SERVER_PORT}:8000"
    environment:
      PYTHONUNBUFFERED: ${PYTHONUNBUFFERED}
    deploy:
      resources:
        limits:
          memory: ${MEMORY_LIMIT}